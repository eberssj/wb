export default class Servico {
    public nome: string
    public valor: number // Valor do serviço
    constructor(nome: string, valor: number) {
        this.nome = nome
        this.valor = valor
    }
}
